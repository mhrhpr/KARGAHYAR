name: KARGAHYAR Android Build
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install -g eas-cli
      - run: cd apps/mobile && npm install
      - run: cd apps/mobile && eas build --platform android --profile preview --non-interactive --json
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}