import {useEffect,useState} from "react"; import {Pressable,SafeAreaView,Text,View} from "react-native"; import {BRAND} from "../lib/brand"; import {canUseBiometric,getBiometricEnabled,setBiometricEnabled} from "../lib/biometric"; import {configureNotificationCategory,configureReminder,getReminderFrequency,ReminderFrequency} from "../lib/reminders";

const frequencies:[ReminderFrequency,string,string][]=[["off","خاموش","هیچ یادآوری ارسال نشود"],["daily","روزانه","هر روز ساعت ۲۰"],["weekly","هفتگی","پنجشنبه ساعت ۲۰"],["hourly","هر ساعت","برای کارگاه‌های حساس؛ قابل خاموش کردن"]];

export default function Security(){const [enabled,setEnabled]=useState(false); const [available,setAvailable]=useState(false); const [frequency,setFrequency]=useState<ReminderFrequency>("daily"); useEffect(()=>{Promise.all([getBiometricEnabled(),canUseBiometric(),configureNotificationCategory()]).then(([e,a])=>{setEnabled(e as boolean);setAvailable(a as boolean);return getReminderFrequency()}).then(setFrequency)},[]); const choose=async(f:ReminderFrequency)=>{setFrequency(f);await configureReminder(f)}; return <SafeAreaView style={{flex:1,backgroundColor:BRAND.bg,padding:20}}><Text style={{color:BRAND.text,fontSize:28,fontWeight:"900"}}>تنظیمات</Text><View style={{marginTop:22,backgroundColor:BRAND.surface,borderWidth:1,borderColor:BRAND.border,borderRadius:18,padding:20}}><Text style={{color:BRAND.text,fontSize:18,fontWeight:"800"}}>ورود سریع</Text><Text style={{color:BRAND.muted,marginTop:6}}>{available?"این دستگاه برای ورود بیومتریک آماده است.":"در این دستگاه بیومتریک در دسترس نیست یا هنوز فعال نشده است."}</Text><Pressable disabled={!available} onPress={async()=>{await setBiometricEnabled(!enabled);setEnabled(!enabled)}} style={{marginTop:18,backgroundColor:enabled?BRAND.accent:BRAND.text,padding:15,borderRadius:13,opacity:available?1:.5}}><Text style={{textAlign:"center",fontWeight:"900",color:enabled?"#fff":BRAND.bg}}>{enabled?"بیومتریک فعال است":"فعال‌سازی ورود سریع"}</Text></Pressable></View><View style={{marginTop:16,backgroundColor:BRAND.surface,borderWidth:1,borderColor:BRAND.border,borderRadius:18,padding:20}}><Text style={{color:BRAND.text,fontSize:18,fontWeight:"800"}}>یادآوری گزارش</Text><Text style={{color:BRAND.muted,marginTop:6}}>تکرار را خودت تنظیم کن تا اعلان‌ها آزاردهنده نشوند.</Text><View style={{gap:9,marginTop:14}}>{frequencies.map(([value,title,desc])=><Pressable key={value} onPress={()=>choose(value)} style={{padding:14,borderRadius:13,borderWidth:1,borderColor:frequency===value?BRAND.accent:BRAND.border,backgroundColor:frequency===value?BRAND.accent:BRAND.bg}}><Text style={{color:"#fff",fontWeight:"900"}}>{title}</Text><Text style={{color:frequency===value?"#EAF2FF":BRAND.muted,marginTop:3}}>{desc}</Text></Pressable>)}</View></View></SafeAreaView>}