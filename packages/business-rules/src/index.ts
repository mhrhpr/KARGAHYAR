export type AttendanceFactor=0|0.5|1; export function workerPayable(dailyWage:number,attendance:AttendanceFactor,overtime:number,advance:number){return Math.max(0,dailyWage*attendance+overtime-advance)} export function contractorAmount(quantity:number,unitRate:number){return Math.max(0,quantity*unitRate)} export function projectCost(labor:number,contractors:number,materials:number,expenses:number){return labor+contractors+materials+expenses} export function stockAlert(current:number,reorderPoint:number){return current<=reorderPoint}